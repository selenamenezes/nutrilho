DB_USER=mtauser
DB_PASSWORD=Jujunior@1009321
DB_HOST=localhost
DB_PORT=3306
DB_NAME=prato_ideal

# Flask (app.py)
FLASK_SECRET_KEY=82b9d1e6e27a4fb28fdf6c306a77de0b9e8c70b733736f74d36c99cbdf6e07f4
# Optional: restrict browser origins (comma-separated). Empty = default CORS (dev-friendly).
# CORS_ORIGINS=http://localhost:8081,http://127.0.0.1:8081
# Optional: set true in production behind HTTPS (ngrok uses HTTPS).
# FLASK_SESSION_COOKIE_SECURE=true

# Expo / React Native: set EXPO_PUBLIC_API_URL in nutrisyinc/.env (project root), not here.
# If you set CORS_ORIGINS below, include your ngrok HTTPS origin (see backend/.env.example).
